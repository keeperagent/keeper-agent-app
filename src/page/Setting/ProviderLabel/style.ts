import styled from "styled-components";

const Wrapper = styled.span`
  display: inline-flex;
  align-items: center;
  gap: 1rem;

  img {
    width: 16px;
    height: 16px;
    object-fit: contain;
    border-radius: 3px;
  }
`;

export { Wrapper };
