import styled from "styled-components";

const Wrapper = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;

  .bg {
    cursor: pointer;
    width: 1.5rem;
    height: 1.5rem;
    border-radius: 3px;
  }
`;

export { Wrapper };
